export enum MeetingType {
  date = 'date',
  meal = 'meal',
}

export enum MealType {
  lunch = 'lunch',
  dinner = 'dinner',
}

export enum MeetingStatus {
  inProgress = 'inProgress',
  done = 'done',
}
